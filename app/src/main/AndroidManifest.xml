<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.conversorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TransmisionActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".MainActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".AlmacenamientoActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".LongitudActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".MasaActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".TiempoActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>
        <activity android:name=".VolumenActivity"
            android:parentActivityName=".HomeActivity"
            ></activity>

        <activity android:name=".HomeActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>